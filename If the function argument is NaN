Напишите функцию multBy5, которая принимает величину value в качестве аргумента, и если это NaN, возвращает строку "Value is NaN", иначе умножает величину на 5.

Примеры:

функция multBy5(3) должна возвратить 15;
функция multBy5(0) должна возвратить 0;
функция multBy5(true) должна возвратить 5;
функция multBy5(false) должна возвратить 0;
функция multBy5(null) должна возвратить 0;
функция multBy5('a') должна возвратить NaN;
функция multBy5(undefined) должна возвратить NaN.
функция multBy5('a' * 5) должна возвратить "Value is NaN";
функция multBy5(NaN) должна возвратить "Value is NaN";
функция multBy5(+'hello') должна возвратить "Value is NaN".

Write the function multBy5, which takes the value as an argument, and if it is NaN, returns the string "Value is NaN", otherwise multiplies the value by 5.

Examples:

the function multBy5(3) should return 15;
the mult by 5(0) function should return 0;
the multBy5(true) function should return 5;
the multBy5(false) function should return 0;
the multBy5(null) function should return 0;
the multBy5('a') function should return NaN;
the multBy5(undefined) function should return NaN.
the mult by 5('a' * 5) function should return "Value is NaN";
the multBy5(NaN) function should return "Value is NaN";
the multBy5(+'hello') function should return "Value is NaN".

function multBy5(value){
 return Number.isNaN(value) ? "Value is NaN" : value * 5; 
}
